# Direct dependencies for KIS. Transitive deps resolved by pip.
typer==0.26.8
rich==15.0.0
fastembed==0.8.0
lancedb==0.34.0
pymupdf==1.28.0
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5

# Web UI (app.py). CLI (kis.py) does not require this.
# Floor, not exact pin: app.py uses only long-stable APIs (tabs, metric,
# progress(text=), :color[] markdown, st.rerun) available since ~1.30.
streamlit>=1.30
